import React from 'react'
import { CommonStepProps } from '.'
import { usePlateFormContext } from '@/providers/form/form-provider'

const Review = ({ swiper }: CommonStepProps) => {
  const {} = usePlateFormContext();

  return (
    <div>Review</div>
  )
}

export default Review